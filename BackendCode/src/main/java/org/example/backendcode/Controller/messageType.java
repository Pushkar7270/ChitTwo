package org.example.backendcode.Controller;


/**
 * the chat will have either the message(CHAT) , a member join ping or a member leave ping
 */
public enum messageType {

    CHAT,
    JOIN,
    LEAVE,
}

